ReportType=AssetReport
Description=A report showing the file size and dimensions of textures before and after import to Unity. Use the asset type filter to report only assets with the type Texture2D.

AssetFilterOptions=Texture2D;